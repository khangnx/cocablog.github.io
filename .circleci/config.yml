version: 2
jobs:
  build:
    docker:
      - image: andthensome/alpine-hugo-git-bash
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
          build:
    branches:
      only:
        - master